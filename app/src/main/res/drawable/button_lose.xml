<!-- res/drawable/button_background.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#9B1717" /> <!-- Change the color to your desired pressed color -->
            <stroke
                android:width="1dp"
                android:color="#FFFFFF" /> <!-- White stroke around the button -->
            <corners android:radius="0dp" /> <!-- Optional: Set corner radius -->
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="@color/banker_button" /> <!-- Change the color to your default color -->
            <stroke
                android:width="1dp"
                android:color="#FFFFFF" /> <!-- White stroke around the button -->
            <corners android:radius="0dp" /> <!-- Optional: Set corner radius -->
        </shape>
    </item>
</selector>
